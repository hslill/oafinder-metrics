{
  "name": "open-access-script",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build:libguide": "node scripts/build-libguide.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "express": "^5.2.1"
  }
}

{
  "scripts": {
    "build:editor": "node scripts/build-editor.js",
    "build:libguide": "node scripts/build-libguide.js",
    "build": "npm run build:editor && npm run build:libguide"
  }
}
